spring:
  datasource:
    url: jdbc:h2:mem:jpa-csv
    username: sa
    password: sa

  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
    generate-ddl: true

  application.name: lib-test-application