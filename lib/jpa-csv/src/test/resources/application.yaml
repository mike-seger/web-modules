spring:
  datasource:
    url: jdbc:h2:mem:jpa-csv;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa

  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
    generate-ddl: true

  application.name: lib-test-application